apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuploy-web
spec:
  selector:
    matchLabels:
      app: kuploy-web-deployment
  template:
    metadata:
      labels:
        app: kuploy-web
    spec:
      containers:
        - name: kuploy-web-deploy
          image: kuploy/kuploy-web
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 300m
              memory : 300Mi
